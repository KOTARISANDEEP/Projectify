{
  "name": "projectify-netlify",
  "version": "1.0.0",
  "description": "Projectify Full Stack Application for Netlify",
  "scripts": {
    "build": "cd project && npm run build",
    "dev": "cd project && npm run dev",
    "install-deps": "cd project && npm install && cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
